name: Integration Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUST_LOG: info

jobs:
  build:
    name: Build Test Binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-directories: crates/deploy/tests/kona/target
      - uses: taiki-e/install-action@nextest

      - name: Build kona-node binary
        run: cargo build --release --bin kona-node
        working-directory: crates/deploy/tests/kona

      - name: Archive integration test binaries
        run: cargo nextest archive --tests --archive-file nextest-archive.tar.zst

      - uses: actions/upload-artifact@v4
        with:
          name: nextest-archive
          path: nextest-archive.tar.zst
          retention-days: 1

      - uses: actions/upload-artifact@v4
        with:
          name: kona-node-binary
          path: crates/deploy/tests/kona/target/release/kona-node
          retention-days: 1

  bench:
    name: Benchmark
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release

      - name: Run benchmark
        run: ./target/release/kupcake bench --iterations 3 --warmup 1 --output bench-results.toml

      - name: Print results
        run: cat bench-results.toml

      - uses: actions/upload-artifact@v4
        with:
          name: bench-results
          path: bench-results.toml
          retention-days: 30

  all-tests:
    name: All Tests (${{ matrix.partition }}/6)
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        partition: [1, 2, 3, 4, 5, 6]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: taiki-e/install-action@nextest

      - uses: actions/download-artifact@v4
        with:
          name: nextest-archive

      - uses: actions/download-artifact@v4
        with:
          name: kona-node-binary
          path: kona-node-binary

      - name: Run integration tests (shard ${{ matrix.partition }}/6)
        env:
          KUPCAKE_TEST_TMPDIR: ${{ github.workspace }}
          KONA_NODE_BINARY: ${{ github.workspace }}/kona-node-binary/kona-node
        run: |
          cargo nextest run \
            --archive-file nextest-archive.tar.zst \
            --no-capture \
            --partition count:${{ matrix.partition }}/6 \
            -E 'not binary(network_redeploy_test)'
